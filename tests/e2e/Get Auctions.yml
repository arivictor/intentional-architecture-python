info:
  name: Get Auctions
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/auctions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
