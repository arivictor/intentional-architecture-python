info:
  name: Create Auction
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/auctions
  body:
    type: json
    data: |-
      {
        "item_id": "book",
        "starting_price": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
